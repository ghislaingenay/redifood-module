export enum EPaymentType {
  CASH = 'cash',
  CARD = 'card',
}

export enum EPaymentStatus {
  CANCELLED = 'cancelled',
  REJECTED = 'rejected',
  AWAITING = 'awaiting payment',
  COMPLETED = 'completed',
}
